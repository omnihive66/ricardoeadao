{
  "version": 2,
  "installCommand": "npm install -g pnpm@10.4.1 --force && cd site && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd site && pnpm run build:client",
  "outputDirectory": "site/dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
