{
  "version": 2,
  "buildCommand": "cd site && npm install -g pnpm@10.4.1 && pnpm install --no-frozen-lockfile && pnpm run build",
  "outputDirectory": "site/dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
